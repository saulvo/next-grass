import { Scene } from "@/components";
import React from "react";

const Home = () => {
  return (
    <main className="w-full h-screen bg-black">
      <Scene />
    </main>
  );
};

export default Home;
